import 'package:flutter/material.dart';

class ChangeThumbnailScreen extends StatelessWidget {
  const ChangeThumbnailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [],
    );
  }
}
